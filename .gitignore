# Build outputs
dist/
build/
out/
*.zip
*.crx
*.xpi

# npm artifacts (this repo must NEVER contain any of these; see CONTRIBUTING.md)
package.json
package-lock.json
yarn.lock
pnpm-lock.yaml
node_modules/
.npm
.yarn

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~
*.bak

# Logs / temp
*.log
*.tmp
.env
.env.local
.env.*.local

# Coverage / test artifacts
coverage/
*.lcov
.nyc_output/

# Local-only notes (not part of the published repo)
NOTES.md
TODO-local.md

# ML model files (build artifacts, regenerated from ml-artifacts/dist_5way/)
src/ml_model/
lens_ml_build/

# Local testing servers and outputs
/tmp/lens_*
ml-artifacts/models/minilm_l12_tier3/model.safetensors
ml-artifacts/dist_tier3_v2/model.onnx
ml-artifacts/dist_tier3_v2/model_fp32.onnx
ml-artifacts/dist_tier3_v2/model__int8.onnx

# Day 2 test harness - mock-backend JSONL stream (regenerated on every run).
# Don't ship the local debugging output; the source-of-truth is the
# telemetry endpoint, not the test fixture.
test/mock-output/

# Python bytecode from the Go-adjacent harness directory.
harness/__pycache__/
